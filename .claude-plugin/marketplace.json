{
  "name": "agent-sync",
  "owner": {
    "name": "ssheleg",
    "url": "https://github.com/ssheleg"
  },
  "description": "Let concurrent coding agents share one project without colliding — leases, ID reservation, a run journal and a generated board, over a pluggable knowledge cloud.",
  "plugins": [
    {
      "name": "agent-sync",
      "displayName": "Agent Sync",
      "source": "./plugins/agent-sync",
      "description": "Coordination layer for multi-agent repositories. Gives task-pipeline runs leases with a TTL, race-free ID reservation, a cross-repo signal feed, an append-only run journal and a machine-generated read-only board. A lease is decided by a primitive that genuinely has compare-and-swap — an atomic file create on one machine, a pushed git ref across machines — never by the knowledge base, which loses concurrent writes; the store carries the record and the ID allocation, which is positional over the merged log. The knowledge backend is a pluggable adapter (Outline, filesystem) declaring its own capabilities and degrading out loud. Ships Claude Code hooks that deny edits to guarded registry files without a live lease.",
      "version": "1.12.0",
      "author": {
        "name": "ssheleg",
        "url": "https://x.com/sshlg93"
      },
      "repository": "https://github.com/ssheleg/agent-sync",
      "homepage": "https://github.com/ssheleg/agent-sync",
      "license": "MIT",
      "keywords": [
        "multi-agent",
        "coordination",
        "lease",
        "knowledge-base",
        "task-pipeline",
        "monorepo",
        "submodules"
      ],
      "category": "development"
    }
  ]
}
